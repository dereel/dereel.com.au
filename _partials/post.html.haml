---
---
%article.post
  %header.header
    %h2.title
      %a{:href=>page.post.url}= page.post.title
  .body.clearfix
    ~ page.post.content
  .footer
    %span.author
      Posted by
      %i.fa.fa-user
      = page.post.author
    %span.dateinline
      on
      %i.fa.fa-calendar
      #{page.post.date.strftime('%b %d, %Y')}
    %span.permalink
      |
      %i.fa.fa-link
      %a{:href=>page.post.url} permalink
    - if site.disqus
      %span.comments
        |
        %i.fa.fa-comments
        #{page.post.disqus_comments_link}
  %hr
  - if page.include_comments
    #comments
      - if site.disqus
        = page.post.disqus_comments
  - if (page.include_comments or page.include_comments_count) and site.disqus
    = page.parent.disqus_comments_count
