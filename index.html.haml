---
layout: base
---

- if site.posts
  - for post in site.posts[0..2]
    .post
      %h3 
        %a{:href=>post.url}= post.title
      .content
        = post.content
      %p{:style=>"text-align:right;padding-top:7px;"}
        %small
          Posted by
          = succeed ',' do
            = post.author
          = post.date.strftime( '%d %B %Y' )

%p{:style=>"padding-top: 20px;"}
  %a.btn.btn-primary.btn-large{ :href=>"#{site.base_url}/news/" }
    %i.icon-info-sign.icon-white
    More posts &raquo;
